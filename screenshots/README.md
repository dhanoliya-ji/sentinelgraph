# Screenshots

Drop the six images referenced by the root README here, with these exact names:

| File | What to capture |
|---|---|
| `01-dashboard.png` | Full app: stat tiles, whole graph, legend visible |
| `02-ring-detection.png` | Ring detector run, headline and cycle visible |
| `03-why-flagged.png` | `ACC_1101` selected, **Why flagged** tab, a reason highlighted in the graph |
| `04-shared-infrastructure.png` | Shared-infrastructure detector showing the six-account cluster |
| `05-trace-path.png` | Trace from `ACC_1301` to `ACC_1101` |
| `06-offline-state.png` | The offline banner and a retryable error (stop the backend, reload) |

Capture at 1920×1080 with the browser at 100% zoom, against the seeded dataset
(`python seed/seed_database.py --reset`) so the numbers match the README.
